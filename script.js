const cancel = document.queryselector('.cancel');
const hamburger = document.querySelector('.hamburger');
const navmenu = document.querySelector('.nav-menu');
const menu = document.querySelectorAll('h-menu');
function display () {
  if()
}